import './app.css';

export default function App() {
  return (
    <main className="min-h-screen bg-[#151821] p-6 text-white">
      <nav className="flex h-10 items-center border border-white/10 bg-[#202636] px-4 text-sm font-medium">
        League Live Desk
      </nav>

      <section className="mt-8 max-w-2xl">
        <h1 className="text-2xl font-semibold">League Live Desk</h1>
        <p className="mt-3 text-sm text-white/70">
          Overwolf data client is ready to launch.
        </p>
      </section>
    </main>
  );
}
